{
  "name": "@ai-video/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Cross-cutting types shared between server, UI, and desktop shell (project schema, error envelope, etc.).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*"
  },
  "scripts": {
    "test": "vitest run --dir src"
  }
}
