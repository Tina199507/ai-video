/**
 * Chat automation contracts — types and defaults routed through impl for a stable boundary.
 */
export type {
  ScrapedModel,
  ChatResult,
  ChatAutomationOptions,
  DetectedSelectors,
  DetectedVideoSelectors,
  ModelOption,
} from './contracts.impl.js';

export { DEFAULT_CHAT_AUTOMATION_OPTIONS } from './contracts.impl.js';
