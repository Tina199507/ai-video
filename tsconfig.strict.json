{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noUncheckedIndexedAccess": true,
    "noPropertyAccessFromIndexSignature": true,
    "exactOptionalPropertyTypes": false
  },
  "//": "Progressive strict-mode playground. Run: npx tsc -p tsconfig.strict.json. Fix the files you touch, then migrate flags into tsconfig.json once the counters hit zero."
}
